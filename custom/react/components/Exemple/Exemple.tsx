import React, { FC } from "react";
//import { useProduct } from "vtex.product-context";

export const Exemple: FC = () => {
    //  const productContextValue = useProduct();
    //console.log("Contexto: ", productContextValue);

    return (
        <div>
            <h2>algum texto</h2>
        </div>
    );
};
